import 'package:flutter/material.dart';

class ParadiseColors {
  // 画像の「塔」：信頼感と落ち着きのあるメインカラー
  static const Color primaryTeal = Color(0xFF008B8B);

  // 画像の「黄金の環」：特別なアクションや強調に使うアクセント
  static const Color accentGold = Color(0xFFD4AF37);
  static const Color subaccentGold = Color(0xFFFAD56A);

  // 画像の「空」：広がりと清潔感を感じさせる背景色
  static const Color skyBackground = Color(0xFFF0F8FF);

  // テキスト用：読みやすさを確保するための深い色
  static const Color deepText = Color(0xFF004D4D);

  // --- 新規追加：地面・背景・岩の色 ---

  // 地面の青：澄んだ氷のような鮮やかなライトブルー
  static const Color groundBlue = Color(0xFF87CEEB);

  // 地面の白：雪や雲の反射を感じさせるオフホワイト
  static const Color groundWhite = Color(0xFFF5FEFF);

  // 背景の空（上部）：深みのあるグラデーション用のブルー
  static const Color skyDeepBlue = Color(0xFF4682B4);

  // 背景の雲：柔らかい光を含んだ淡いグレー
  static const Color cloudGrey = Color(0xFFE6E6FA);

  // 浮遊する岩：氷結した岩肌を思わせる冷たいブルーグレー
  static const Color crystalRock = Color(0xFFB0C4DE);
}